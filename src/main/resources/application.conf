###########################################
# Project Reference Configuration File	  #
###########################################

# This is the reference configuration file that contains all the default settings.
# Make your edits/overrides in your application.conf.

akka {
  loglevel = DEBUG
}

hadoop {
  winutils = "D:/opensource/hadoop-2.7.1/winutils"
}

data {

	movies { file = "data/ml-20m/movies.csv" }
	ratings { file = "data/ml-20m/ratings.csv" }
	users { file = "data/ml-20m/users.csv" }
	tags { file = "data/ml-20m/tags.csv" }
}
